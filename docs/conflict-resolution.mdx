---
title: "Conflict Resolution"
description: "Resolve merge and rebase conflicts without leaving Commit+."
---

# Conflict Resolution

When a merge, rebase, cherry-pick, or revert produces conflicts, Commit+ opens the **conflict resolution** view.

## What you get

- A visual diff viewer with inline conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`).
- Stage-resolution controls to accept ours, theirs, or both per hunk.
- The ability to **abort** the merge or rebase and return to your prior state.

## Undo

Aborts and resolved merges are undoable. Use `Cmd+Z` to roll back.